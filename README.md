# Install dependencies
pip install -r requirements.txt

# Run Python 
python app.py

# Example cURL request
curl -H "Authorization: Bearer MySuperSecretToken123" "http://localhost:8000/search?query=coffee+shop+Batam"